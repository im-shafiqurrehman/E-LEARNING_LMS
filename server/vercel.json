{
  "version": 2,
  "builds": [
    {
      "src": "server/app.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/app.ts"
    },
    {
      "src": "/(.*)",
      "dest": "server/app.ts"
    }
  ]
}